# Disable Nixpacks by pointing to Docker strategy (Railway prefers Dockerfile if present)
# This file is intentionally minimal to avoid Nixpacks cache mounts which caused EBUSY.
[variables]
DISABLE_NIXPACKS = "true"